const GameBoard = () => {
  const placeShip = () => {};
  const receiveAttack = () => {};
  const hasAllSunk = () => {};

  return { placeShip, receiveAttack, hasAllSunk };
};

export default GameBoard;
